#!/bin/sh

if [ "$1" = "--on" ]; then
amixer set Speaker off
if [ -f /etc/local/tvout.conf ]; then
tvout --`cat /etc/local/tvout.conf`
else
tvout --pal-60
fi
elif [ "$1" = "--off" ]; then
tvout --off
amixer set Speaker on
fi
